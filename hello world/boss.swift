import SwiftUI
struct boss:View {
    var body:some View{
        Text("shir")
            .fontWeight(.bold)
            .font(.title).frame(width: 1000, height: 1000)
            .background(Color(.systemPink))
            .foregroundColor(Color(.white))
            
}
}
